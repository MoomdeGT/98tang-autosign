services:
  autosign:
    build: .
    image: 98tang-autosign:latest
    container_name: 98tang-autosign
    volumes:
      - ./data/config.env:/app/config.env
      - ./data/logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
    #deploy:
      #resources:
        #limits:
          #memory: 2G
    network_mode: bridge
